from parser import CustomParser

if __name__ == '__main__':
	# TODO: Fix PASSWORD_MANAGER_DEFAULT_USERNAME env variable not being able
	#  to be parsed when the script is run via binaries
	CustomParser()
